{% extends "NewsBundle::layout.html.twig" %}

{% block content %}
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <li>
                                <a href="{{ path('logout') }}">
                                    Выход
                                </a>
                            </li>
     {% endif %}
     {% if is_granted('ROLE_ADMIN') %}
         я админ!
    {% endif %}

     Hello {{user}} !
{% endblock %}
