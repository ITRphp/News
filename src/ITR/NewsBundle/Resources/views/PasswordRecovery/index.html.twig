{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PasswordRecovery list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Accesshash</th>
                <th>Expires</th>
                <th>User</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_passwordrecovery_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.accessHash }}</td>
                <td>{% if entity.expires %}{{ entity.expires|date('Y-m-d H:i:s') }}{% endif %}</td>
                <th>{{ entity.user }}</th>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_passwordrecovery_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_passwordrecovery_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_passwordrecovery_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
