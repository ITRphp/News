{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PasswordRecovery</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Accesshash</th>
                <td>{{ entity.accessHash }}</td>
            </tr>
            <tr>
                <th>Expires</th>
                <td>{{ entity.expires|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ entity.user }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_passwordrecovery') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_passwordrecovery_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
